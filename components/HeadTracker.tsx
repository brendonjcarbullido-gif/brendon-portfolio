'use client'

/**
 * MediaPipe face mesh tracking is implemented in `@/lib/hooks/useHeadTracking`.
 * This module is the integration surface named in the project structure spec.
 */
export { useHeadTracking } from '@/lib/hooks/useHeadTracking'
